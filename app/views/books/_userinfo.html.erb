
<% if @book.errors.any? %>
  <%= @book.errors.count %>error prohibited this obj from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>


    <p id="notice"></p>
    <div class="container px-5 px-sm">
      <div class="row">
        <div class="col-md-3">

          <h2>User info</h2><br />
          <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "/assets/image/no_image.jpg" %><br>


          <taable class="table">
            <tbody>
              <tr></tr>
            <tr>
              <th>name</th>
              <th><%= user.name %></th><br>
            </tr>
            <tr>
              <th>introduction</th>
              <th><%= user.introduction %></th>
            </tr>
            </tbody>
            </taable>

<%= link_to edit_user_path(user), class: "btn btn-primary form-control" do %>
  <i class="fas fa-user-cog"></i>

<% end %>




              <h2 class="mt-3">New book</h2>
                <%= form_with model: book, local:true do |f| %>


                <div class="form-group">
                <label for="book_title">Title</label><br />
                  <%= f.text_field :title %></div><br />

                <div class="form-group">
                <label for="book_title">Opinion</label><br />
                  <%= f.text_area :body %>
                </div>
                  <%= f.submit 'Create Book' %>

               <% end %>
        </div>
